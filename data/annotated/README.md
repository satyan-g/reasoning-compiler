# annotated

Manual annotations with ground-truth FRL. **Committed to git** — these represent human judgment.

Format: JSONL with fields: problem_id, nl_text, entities, constraints, implicit_constraints, solution
